rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access goes through the backend (Admin SDK, which bypasses these
    // rules). Deny every direct client read/write.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
